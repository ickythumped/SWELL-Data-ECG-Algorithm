function [dsquare_lambda_mat] = dsquare_lambda(f_val, integ_f_val, ...
    d_lambda_vec, integ_df_vec, dsquaref_mat, integ_dfsquare_mat) 
%UNTITLED5 Summary of this function goes here
%   Detailed explanation goes here

expr1 = 1/(1 - integ_f_val);

for a = 1:nparams+2
    for b = 1:nparams+2
        term1 = (2.*expr1).*(d_lambda_vec(b).*integ_df_vec(a));
        term2 = dsquaref_mat(a,b).*expr1;
        term3 = (expr1^2).*
    end
end


end

